diff --git a/index.html b/index.html
index 8e7b529..35c96c9 100644
--- a/index.html
+++ b/index.html
@@ -4466,7 +4466,7 @@ body.light .nr-state-unread { color:#059669; }
   <div style="max-width:780px;margin:0 auto;padding:0 5%;font-family:'Inter',sans-serif;">
 
     <div style="display:flex;justify-content:space-between;align-items:center;margin-bottom:16px;">
-      <div><div style="font-size:22px;font-weight:700;color:white;font-family:'Space Grotesk',sans-serif;">Weekly Challenges</div><div style="font-size:12px;color:#5a7a9a;margin-top:3px;">Complete challenges to earn XP. Resets every Monday.</div></div>
+      <div><div style="font-size:22px;font-weight:700;color:white;font-family:'Space Grotesk',sans-serif;">Weekly Challenges</div><div id="ch-subtitle" style="font-size:12px;color:#5a7a9a;margin-top:3px;">Complete challenges and build your weekly progress. Resets every Monday.</div></div>
     </div>
 
     <!-- PROGRESS HERO -->
@@ -4474,14 +4474,14 @@ body.light .nr-state-unread { color:#059669; }
       <div style="display:flex;justify-content:space-between;align-items:center;margin-bottom:12px;flex-wrap:wrap;gap:8px;">
         <div style="display:flex;align-items:center;gap:10px;">
           <div id="ch-prog-text" style="font-size:28px;font-weight:800;color:#2dd4a0;font-family:'Space Grotesk',sans-serif;">0<span style="font-size:14px;color:#5a7a9a;font-weight:400;"> / 5</span></div>
-          <div><div style="font-size:12px;font-weight:600;color:#e8f0f8;">challenges done</div><div style="font-size:11px;color:#5a7a9a;margin-top:1px;" id="ch-xp-earned">+0 XP earned this week</div></div>
+          <div><div style="font-size:12px;font-weight:600;color:#e8f0f8;">challenges done</div><div style="font-size:11px;color:#5a7a9a;margin-top:1px;" id="ch-xp-earned">0 of 5 challenges completed this week</div></div>
         </div>
         <div style="text-align:right;"><div style="font-size:11px;color:#5a7a9a;">Resets in</div><div style="font-size:16px;font-weight:700;color:#f59e0b;font-family:'Space Grotesk',sans-serif;" id="ch-countdown">—</div></div>
       </div>
       <div style="height:8px;background:#0d1520;border-radius:99px;overflow:hidden;">
         <div id="ch-hero-bar" style="width:0%;height:100%;background:linear-gradient(90deg,#2dd4a0,#059669);border-radius:99px;transition:width 0.4s;"></div>
       </div>
-      <div style="display:flex;justify-content:space-between;margin-top:4px;font-size:10px;color:#5a7a9a;"><span id="ch-hero-pct">0% complete</span><span id="ch-hero-remaining">+90 XP remaining</span></div>
+      <div style="display:flex;justify-content:space-between;margin-top:4px;font-size:10px;color:#5a7a9a;"><span id="ch-hero-pct">0% complete</span><span id="ch-hero-remaining">5 challenges remaining</span></div>
     </div>
 
     <!-- CHALLENGES LIST -->
@@ -4490,7 +4490,7 @@ body.light .nr-state-unread { color:#059669; }
     <!-- BONUS REWARD -->
     <div id="ch-bonus-card" style="margin-top:12px;background:rgba(245,158,11,0.04);border:1px solid rgba(245,158,11,0.12);border-radius:10px;padding:12px;display:flex;align-items:center;gap:10px;">
       <div style="font-size:16px;">🏆</div>
-      <div><div style="font-size:12px;font-weight:700;color:#f59e0b;">Complete all 5 = Bonus 25 XP</div><div style="font-size:10px;color:#5a7a9a;margin-top:2px;">Finish every challenge this week for an extra reward</div></div>
+      <div><div id="ch-bonus-heading" style="font-size:12px;font-weight:700;color:#f59e0b;">Complete all weekly challenges</div><div id="ch-bonus-desc" style="font-size:10px;color:#5a7a9a;margin-top:2px;">Finish every challenge this week.</div></div>
       <div style="margin-left:auto;font-size:12px;font-weight:700;color:#5a7a9a;" id="ch-bonus-left">5 left</div>
     </div>
   </div>
@@ -14844,10 +14844,18 @@ function bumpWeekly(key,amount){
   const challenges = getChallenges();
   challenges.forEach(c=>{was[c.id]=(p[c.key]||0)>=c.target;});
   p[key]=(p[key]||0)+(amount||1);saveWeeklyProg(p);
+  const isDemo = String(currentUser.id || '').startsWith('demo-');
   challenges.forEach(c=>{
   if(!was[c.id] && (p[c.key]||0) >= c.target){
-    addXp(c.xp,'goal','Challenge: '+c.title);
-    showToast('🎯 Challenge complete! +'+c.xp+' XP');
+    if(isDemo){
+      addXp(c.xp,'goal','Challenge: '+c.title);
+      showToast('🎯 Challenge complete! +'+c.xp+' XP');
+    } else {
+      // XP1 containment: real accounts must not receive browser-controlled
+      // challenge XP. Progress is still tracked and synced; the reward will
+      // return through a trusted server RPC in a later increment.
+      showToast('🎯 Challenge complete: '+c.title);
+    }
   }
 });
   renderChallenges();
@@ -14862,17 +14870,21 @@ function renderChallenges(){
   const totalXp=challenges.reduce((s,c)=>s+c.xp,0);
   const earnedXp=challenges.filter(c=>(prog[c.key]||0)>=c.target).reduce((s,c)=>s+c.xp,0);
   const pct=Math.round((done/challenges.length)*100);
+  const isDemo = String(currentUser?.id || '').startsWith('demo-');
 
   // Update hero
   const ptxt=document.getElementById('ch-prog-text');if(ptxt)ptxt.innerHTML=done+'<span style="font-size:14px;color:#5a7a9a;font-weight:400;"> / '+challenges.length+'</span>';
-  const xpE=document.getElementById('ch-xp-earned');if(xpE)xpE.textContent='+'+earnedXp+' XP earned this week';
+  const xpE=document.getElementById('ch-xp-earned');if(xpE)xpE.textContent=isDemo?('+'+earnedXp+' XP earned this week'):(done+' of '+challenges.length+' challenges completed this week');
   const heroBar=document.getElementById('ch-hero-bar');if(heroBar)heroBar.style.width=pct+'%';
   const heroPct=document.getElementById('ch-hero-pct');if(heroPct)heroPct.textContent=pct+'% complete';
-  const heroRem=document.getElementById('ch-hero-remaining');if(heroRem)heroRem.textContent='+'+(totalXp-earnedXp)+' XP remaining';
+  const heroRem=document.getElementById('ch-hero-remaining');if(heroRem){const remN=challenges.length-done;heroRem.textContent=isDemo?('+'+(totalXp-earnedXp)+' XP remaining'):(remN+(remN===1?' challenge remaining':' challenges remaining'));}
+  const subT=document.getElementById('ch-subtitle');if(subT)subT.textContent=isDemo?'Complete challenges to earn XP. Resets every Monday.':'Complete challenges and build your weekly progress. Resets every Monday.';
+  const bH=document.getElementById('ch-bonus-heading');if(bH&&done<challenges.length){bH.textContent=isDemo?'Complete all 5 = Bonus 25 XP':'Complete all weekly challenges';}
+  const bD=document.getElementById('ch-bonus-desc');if(bD)bD.textContent=isDemo?'Finish every challenge this week for an extra reward':'Finish every challenge this week.';
   const bonusLeft=document.getElementById('ch-bonus-left');if(bonusLeft)bonusLeft.textContent=(challenges.length-done)+' left';
   const bonusCard=document.getElementById('ch-bonus-card');
   if(bonusCard){
-    if(done>=challenges.length){bonusCard.style.background='rgba(45,212,160,0.06)';bonusCard.style.borderColor='rgba(45,212,160,0.2)';bonusCard.querySelector('div:nth-child(2) div:first-child').style.color='#2dd4a0';bonusCard.querySelector('div:nth-child(2) div:first-child').textContent='🎉 All complete! +25 XP Bonus earned!';}
+    if(done>=challenges.length){bonusCard.style.background='rgba(45,212,160,0.06)';bonusCard.style.borderColor='rgba(45,212,160,0.2)';bonusCard.querySelector('div:nth-child(2) div:first-child').style.color='#2dd4a0';bonusCard.querySelector('div:nth-child(2) div:first-child').textContent=isDemo?'🎉 All complete! +25 XP Bonus earned!':'🎉 All weekly challenges complete!';}
   }
 
   const icons=['📝','💬','🔥','🎯','✉️'];
@@ -14888,12 +14900,12 @@ function renderChallenges(){
           <div><div class="ch-title">${sanitize(c.title)}</div><div class="ch-desc">${sanitize(c.desc)}</div></div>
         </div>
         <div style="display:flex;align-items:center;gap:4px;">
-          ${isDone?`<span class="ch-done-tag">✓ Done!</span>`:`<span class="ch-xp-tag">+${c.xp} XP</span>`}
+          ${isDone?`<span class="ch-done-tag">✓ Done!</span>`:(isDemo?`<span class="ch-xp-tag">+${c.xp} XP</span>`:`<span class="ch-xp-tag">${cur===0?'In progress':(c.target-cur)+' remaining'}</span>`)}
           ${founder?`<button onclick="editChallenge('${c.id}')" title="Edit" style="background:none;border:1px solid #2d4f6f;color:#5a7a9a;border-radius:6px;cursor:pointer;font-size:10px;padding:3px 7px;flex-shrink:0;">✏️</button>`:''}
         </div>
       </div>
       <div class="ch-prog-bar"><div class="ch-prog-fill" style="width:${cPct}%;${!isDone?'background:linear-gradient(90deg,'+leftColor+','+leftColor+'aa);':''}"></div></div>
-      <div class="ch-prog-labels"><span>${cur} / ${c.target}</span><span>${isDone?'<span style="color:#2dd4a0;font-weight:600;">+'+c.xp+' XP earned</span>':'Not started'}</span></div>
+      <div class="ch-prog-labels"><span>${cur} / ${c.target}</span><span>${isDone?(isDemo?'<span style="color:#2dd4a0;font-weight:600;">+'+c.xp+' XP earned</span>':'<span style="color:#2dd4a0;font-weight:600;">Completed</span>'):'Not started'}</span></div>
     </div>`;
   }).join('');
 }
